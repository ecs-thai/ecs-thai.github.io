# คู่มือเพิ่มเนื้อหาเว็บไซต์ ECS Thailand Section

เว็บไซต์ใช้ `content.json` เป็นรายการเนื้อหาที่นำไปแสดงจริง มีสามส่วนหลักคือ `section`, `meetings` และ `news` หลังแก้ไขข้อมูล ให้สร้างหน้าเว็บใหม่และเผยแพร่ไฟล์ที่ได้ ตัวอย่างใน `meeting-template.json` และ `news-template.json` **ไม่ถูกโหลดเข้าหน้าเว็บ** วันที่ ชื่อ และลิงก์ในตัวอย่างต้องเปลี่ยนก่อนใช้จริง

## 1. หน้าหลักและชื่อไฟล์ถาวร

| หน้า | ไฟล์ |
|---|---|
| หน้าแรก | `index.html` |
| เกี่ยวกับเรา | `about.html` |
| ข่าวและกิจกรรม | `activities.html` |
| คลังการประชุม | `meetings.html` |
| เข้าร่วม ECS | `join.html` |

การประชุมแต่ละครั้งมีหน้า `meeting-{id}.html` เช่น `meeting-2026-09-15.html` ให้เก็บหน้าเดิมไว้เมื่อเพิ่มการประชุมใหม่ สไลด์สาธารณะปัจจุบันอยู่ที่ `slides-2026-09-15.html` ไม่ใช่ `index.html` ส่วนครั้งถัดไปควรใช้ `slides-{id}.html` และเอกสารที่เลือกเผยแพร่ควรมีชื่อเฉพาะครั้ง เช่น `document-{id}-th.html`

วาระภาษาอังกฤษปัจจุบันเปิดเป็นหน้าเว็บโดยตรงที่ `agenda-2026-09-15.html` หากต้องการแก้วาระที่แสดง ให้แก้ `agenda-2026-09-15.qmd` แล้ว render และสร้างเว็บใหม่ ไฟล์ `agenda.md` คงไว้เป็นต้นฉบับอ้างอิงสำหรับลิงก์เดิม

## 2. เพิ่มการประชุมครั้งใหม่

1. คัดลอกออบเจ็กต์จาก `meeting-template.json` เป็นรายการใหม่ในอาร์เรย์ `meetings` ของ `content.json` โดยคั่นรายการด้วยจุลภาค
2. เปลี่ยนข้อมูลตัวอย่างทุกช่องเป็นข้อมูลที่ยืนยันแล้ว ใส่ภาษาไทย (`th`) และอังกฤษ (`en`) ให้มีความหมายตรงกัน
3. เตรียมเอกสารสาธารณะและแบบลงทะเบียนเฉพาะครั้ง แล้วใส่เฉพาะลิงก์ที่พร้อมเปิดจริง
4. คงค่า `"minutes": null` จนกว่าจะมีไฟล์รายงานการประชุมที่พร้อมเผยแพร่
5. สร้างหน้าเว็บใหม่ ระบบจะเพิ่มหน้ารายละเอียดลงในคลังและเรียงการประชุมตามวันที่จากใหม่ไปเก่าโดยอัตโนมัติ

| ช่องข้อมูล | วิธีกรอก |
|---|---|
| `id` | ค่าถาวรไม่ซ้ำ เริ่มด้วย `YYYY-MM-DD` อาจต่อท้ายด้วยอักษรอังกฤษตัวเล็ก ตัวเลข และขีด เช่น `2027-01-01-example` ซึ่งเป็นเพียงตัวอย่าง |
| `date` | วันที่ประชุมจริงในรูป `YYYY-MM-DD` ใช้ปีคริสต์ศักราช |
| `title` | ชื่อการประชุมทั้ง `th` และ `en` |
| `time` | เวลาที่ประกาศพร้อมเขตเวลา ทั้งสองภาษา |
| `venue` | สถานที่และช่องทางเข้าร่วมที่เปิดเผยได้ ทั้งสองภาษา |
| `format` | รูปแบบการประชุม เช่น ออนไลน์หรือผสมผสาน ทั้งสองภาษา |
| `summary` | วัตถุประสงค์สั้น ๆ ทั้งสองภาษา ไม่เขียนข้อเสนอเป็นมติหรือผลสำเร็จ |
| `materials` | รายการเอกสารสาธารณะที่มีอยู่จริง ใช้ `[]` ได้หากยังไม่มีเอกสาร |
| `registration_url` | URL แบบลงทะเบียนสาธารณะเฉพาะครั้ง หรือ `null` หากยังไม่มี |
| `registration_qr` | ชื่อไฟล์ QR ของครั้งนี้ในโฟลเดอร์หลัก หรือ `null` หากยังไม่มี |
| `minutes` | `null` หรือข้อมูลไฟล์รายงานการประชุมตามหัวข้อ 4 |

แต่ละรายการใน `materials` มี `kind`, `title`, `detail`, `url` และ `type` โดย `title` กับ `detail` ต้องมีทั้ง `th` และ `en` ค่า `kind` ใช้บอกประเภท เช่น `slides`, `document`, `agenda` หรือ `demo`; `type` คือป้ายสั้นที่แสดง เช่น `WEB`, `PDF`, `TH` หรือ `DEMO`

ลิงก์ภายในใช้ชื่อไฟล์ เช่น `slides-2026-09-15.html` ไม่ขึ้นต้นด้วย `/` และไม่ใช้ `../` ส่วนลิงก์ภายนอกใช้ `https://` เว็บไซต์รองรับ `mailto:` สำหรับอีเมลด้วย โปรแกรมตรวจการมีอยู่ของไฟล์เอกสาร รายงานการประชุม และ QR ที่อ้างอิง แต่ยังต้องเปิดตรวจลิงก์ภายนอกเอง

หน้าแรกเลือกการประชุมที่มีวันที่ล่าสุด และแนะนำเอกสารสามรายการแรกของการประชุมนั้น ควรจัดลำดับเอกสารให้เหมาะกับผู้ชม รายการใน `content.json` ไม่มีสถานะซ่อนร่าง: เมื่อเพิ่มแล้วสร้างและเผยแพร่เว็บ รายการนั้นจะปรากฏต่อสาธารณะ

## 3. เตรียมสไลด์ เอกสาร และการลงทะเบียน

สำหรับสไลด์ใหม่ ให้สร้าง `slides-{id}.qmd` ที่มีเนื้อหาสาธารณะของการประชุมครั้งนั้น กำหนด `format: revealjs` และ `output-file: slides-{id}.html` แล้วเพิ่มชื่อไฟล์ลงใน `project.render` ของ `_quarto.yml` หากมีเอกสารสาธารณะ Quarto เพิ่ม ให้ตั้งชื่อเฉพาะครั้งและเพิ่มในรายการนี้ด้วย ใส่ชื่อ HTML ที่จะได้ใน `materials` ของการประชุมนั้น

หากใช้สไลด์ปี 2026 เป็นตัวอย่าง ให้เปลี่ยนชื่อประชุม วันที่ เวลา สถานที่ ลิงก์ เอกสาร และ QR ในสำเนาใหม่ รวมถึงคำอธิบายใน header ที่ยังระบุงานปี 2026 ควรใช้ header เฉพาะงานใหม่แทนการแก้ข้อมูลของเอกสารเก่าย้อนหลัง

รันจากโฟลเดอร์โครงการตามลำดับ:

```sh
quarto render
python3 build_site.py
```

Quarto สร้างสไลด์และเอกสารสาธารณะไว้ใน `published/` จากนั้น Python จะคัดลอก HTML ภายในที่ถูกอ้างอิงใน `materials` หรือ `minutes` ของทุกการประชุมมายังโฟลเดอร์หลักโดยอัตโนมัติ แล้วสร้างหน้าหลักและหน้าการประชุม ชื่อหน้าหลักและหน้ารายละเอียดการประชุมเป็นชื่อสงวน ไม่ใช้เป็นชื่อไฟล์เอกสาร อย่าสร้างสไลด์ชื่อ `index.qmd` หรือคัดลอก HTML สไลด์ทับ `index.html`

วาระภาษาอังกฤษ `agenda-2026-09-15.qmd` อยู่ในรายการ render เช่นกัน โดยสร้างเป็น HTML เอกสารปกติ ส่วนสไลด์สร้างเป็น reveal.js สำหรับวาระครั้งใหม่ สามารถใช้ชื่อ `agenda-{id}.qmd` และ `agenda-{id}.html` แล้วเชื่อมไฟล์ HTML ใน `materials` ของครั้งนั้น

ไฟล์ PDF รูป QR และเอกสารที่ไม่ได้เป็น HTML ต้องเตรียมไว้ในโฟลเดอร์หลักก่อนสร้างเว็บ หากทำ HTML ด้วยวิธีอื่น สามารถวางไฟล์นั้นในโฟลเดอร์หลักได้โดยตรง แต่ถ้ามี HTML ชื่อเดียวกันใน `published/` ไฟล์ที่ render จะถูกคัดลอกมาแทน จึงควรแก้จากต้นฉบับให้ตรงกัน

สร้างแบบฟอร์ม Notion สาธารณะแยกสำหรับการประชุมแต่ละครั้ง และเก็บคำตอบในพื้นที่ส่วนตัวของผู้จัด ใส่ลิงก์แบบฟอร์มนั้นใน `registration_url` แยกความยินยอมให้ติดต่อในอนาคตเป็นทางเลือก ไม่ใส่ลิงก์ฐานข้อมูลส่วนตัวหรือรายชื่อผู้ตอบในเว็บไซต์

หากต้องการ QR ให้สร้างจาก `registration_url` ของงานนั้น ตั้งชื่อเฉพาะครั้ง เช่น `registration-2027-01-01-example-qr.png` แล้วใส่ชื่อไฟล์ใน `registration_qr` เมื่อมีทั้ง URL และ QR หน้าการประชุมจะแสดงให้ หากไม่มี QR ให้ใช้ `null` และแสดงปุ่มลงทะเบียนตามปกติ ตรวจด้วยการสแกนว่า QR และปุ่มเปิดแบบฟอร์มเดียวกัน รูป `registration-qr.png` เดิมเป็นของวันที่ 15 กันยายน 2026 ไม่ใช้ซ้ำเป็น QR งานใหม่

## 4. เพิ่มรายงานการประชุมจริง

เตรียมไฟล์ HTML หรือ PDF ที่พร้อมเผยแพร่ก่อน แล้วเปลี่ยน `minutes` จาก `null` เป็นออบเจ็กต์ ตัวอย่างต่อไปนี้ใช้ชื่อและวันที่สมมติ ต้องแทนด้วยข้อมูลจริง:

```json
{
  "url": "minutes-2027-01-01-example-draft.pdf",
  "status": "draft",
  "published_on": "2027-01-02"
}
```

- `url` ต้องเปิดไฟล์ที่มีอยู่จริงและเป็นฉบับที่ตั้งใจเผยแพร่ วาง PDF ในโฟลเดอร์หลัก ส่วน HTML ที่ render ไว้ใน `published/` จะถูกคัดลอกเมื่อสร้างเว็บ
- `status` ใช้ได้สองค่า: `draft` คือฉบับร่าง และ `approved` คือฉบับรับรอง ใช้ `approved` เมื่อมีการรับรองจริงแล้ว
- `published_on` คือวันที่เผยแพร่ฉบับนั้นในรูป `YYYY-MM-DD` หากยังไม่ทราบ ให้เว้นช่องนี้ได้

เมื่อสร้างเว็บใหม่ หน้าการประชุมและคลังจะแสดงลิงก์พร้อมสถานะ ตัวกรอง “มีรายงานการประชุม” รวมทั้งฉบับร่างและฉบับรับรองที่เผยแพร่ไฟล์แล้ว รายงานต้องอ้างอิงสิ่งที่เกิดขึ้นในการประชุมจริง ทั้งผู้เข้าร่วม มติ และผู้รับผิดชอบที่ตกลงกัน

## 5. เพิ่มข่าวหรือประกาศกิจกรรม

คัดลอกออบเจ็กต์จาก `news-template.json` ลงในอาร์เรย์ `news` ของ `content.json` แล้วเปลี่ยน `id`, `date`, `category`, `title`, `summary` และ `url` โดย `category`, `title` และ `summary` ต้องมีทั้งสองภาษา ใช้ `id` ไม่ซ้ำเพื่ออ้างอิงข่าวได้ชัดเจน วันที่ในข่าวคือวันที่ของประกาศ ไม่จำเป็นต้องตรงกับวันจัดกิจกรรม

`url` ต้องชี้ไปยังหน้ารายละเอียดที่มีอยู่แล้ว เช่น `meeting-{id}.html` หรือหน้าประกาศสาธารณะภายนอก โปรแกรมสร้างรายการข่าวบนหน้าแรกและหน้า `activities.html` แต่ **ไม่ได้สร้างหน้าข่าวแยกจาก `id` ของข่าว** หากต้องการหน้ารายละเอียดกิจกรรมใหม่ ให้เตรียมหน้านั้นก่อน

ข่าวเรียงจากวันที่ใหม่ไปเก่า และหน้าแรกแสดงสามรายการแรก โปรแกรมยังไม่ได้ตรวจว่าลิงก์ข่าวภายในมีไฟล์จริง จึงต้องเปิดทุกลิงก์ข่าวก่อนเผยแพร่ อย่าเปลี่ยนแนวคิดกิจกรรมที่ยังหารืออยู่ให้เป็นกิจกรรมที่ยืนยันแล้ว หรือใส่วัน ผู้ร่วมจัด และผลลัพธ์ที่ยังไม่มีข้อมูลรองรับ

## 6. สร้าง ตรวจ และเผยแพร่

หากแก้เฉพาะ `content.json` และเอกสารที่จำเป็นมีอยู่แล้ว ให้รัน `python3 build_site.py` หากแก้สไลด์หรือเอกสาร Quarto ให้รัน `quarto render` ก่อน หากคำสั่งแจ้งข้อผิดพลาด ให้แก้และสร้างใหม่สำเร็จก่อนเผยแพร่ไฟล์

เปิดดูเว็บไซต์จากโฟลเดอร์โครงการได้ด้วย:

```sh
python3 -m http.server 8000
```

เปิด `http://localhost:8000/` แล้วตรวจทั้งสองภาษา วันที่ เวลา ลิงก์ลงทะเบียน เอกสารทุกชิ้น และสถานะรายงานการประชุม ควรเก็บ `id` และชื่อไฟล์คงเดิมเมื่อเผยแพร่แล้ว เพื่อให้ลิงก์ที่ส่งให้สมาชิกยังใช้งานได้

เผยแพร่ไฟล์สาธารณะใน **โฟลเดอร์หลักของโครงการ** ไปยังโฟลเดอร์หลักของ repository ที่ใช้เปิด GitHub Pages รวม HTML ที่สร้างแล้ว `site.css`, `site.js`, QR และเอกสารที่เชื่อมโยงไว้ คง `.nojekyll` และเก็บต้นฉบับสาธารณะ `content.json`, `build_site.py`, `.qmd`, ไฟล์ตั้งค่า รูปแบบ คู่มือ และตัวอย่างใน repository เพื่อแก้ไขครั้งถัดไป

ไม่ต้องเผยแพร่ `published/`, `.quarto/`, cache, log, runtime Quarto หรือโฟลเดอร์งานรอบนอก หากอัปโหลดผ่านหน้า GitHub ให้อัปโหลดชุดไฟล์ที่พร้อมครบถ้วน แล้วตรวจ URL จริงหลัง GitHub Pages เผยแพร่เสร็จ

## 7. ลิงก์เดิมและข้อมูลสาธารณะ

`/#/ชื่อสไลด์` และ `/index.html#/ชื่อสไลด์` เดิมถูกส่งต่อไปยัง `slides-2026-09-15.html` พร้อม query และ hash เดิม จึงควรคง ID และลำดับสไลด์ปี 2026 ไว้ ส่วน `/` ที่ไม่มี hash จะแสดงหน้าแรกใหม่ ลิงก์ `vote-demo.html`, `agenda.md` และ QR เดิมยังต้องเปิดได้

เผยแพร่เฉพาะสไลด์และเอกสารที่เลือกสำหรับสาธารณะ เก็บร่างคำพูดประกอบวาระและบันทึกช่วยพูดของประธานไว้นอกโครงการและ repository สาธารณะ ไม่ใส่ไว้ในต้นฉบับ Quarto หรือ HTML ที่จะเผยแพร่ รวมทั้งไม่ใส่รายชื่อหรืออีเมลผู้ลงทะเบียน การส่งออก Notion ส่วนตัว รหัสเข้าประชุม รหัสลงคะแนน หรือกุญแจผู้ดูแลเว็บไซต์ การลงทะเบียนประชุมแยกจากการเป็นสมาชิก ECS และไม่ยืนยันสิทธิ์ลงคะแนน หน้าตัวอย่างลงคะแนนใช้ผู้สมัครสมมติและไม่มีการส่งคะแนนจริง
